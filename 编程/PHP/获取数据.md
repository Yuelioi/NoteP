单条

```php
$card_info = ($db->query("SELECT * FROM `activities` WHERE `ID` = '$activity_id'"))->fetch_assoc()["team_info"];
```

遍历

```php
foreach ($card_info as $key => $value) {

	<img src=" https://qmqxk.yuelili.com//img/nba-logos/ '  . sprintf("%02d", $value) . '.png" ></img>

} 
```

